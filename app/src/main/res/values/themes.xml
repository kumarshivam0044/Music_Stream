<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MusicStream" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MusicStream" parent="Base.Theme.MusicStream" />
    <style name="SplashApi" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/blueDark</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/music</item>
        <item name="postSplashScreenTheme">@style/Theme.MusicStream</item>
















    </style>
</resources>